[package]
name = "zig-lens-sys"
version = "0.1.0"
edition = "2021"
description = "Rust FFI bindings for zig-lens source code analysis library"
license = "MIT"

[build-dependencies]
bindgen = "0.71"

[features]
default = []
